<div class="card col-sm-6 col-md-3">
    <%= link_to image_tag(product.image.url, alt: "product image",
      class: "full-width-image"), [:admin, product] %>
      <h4><b><%= product.name %></b></h4>
      <div class="product-rate"
        data-score="<%= product.average_rate %>">
      </div>
      <span>( <%= pluralize product.rate_count, "reviews" %> )</span>
      <p class="product-price">PRICE: <%= number_to_currency product.price %></p>
      <%= link_to "Delete", admin_product_path(product), class: "  inline-btn btn btn-success", method: :delete, data: { confirm: "Are you sure?" } %>
      <%= link_to "Edit", edit_admin_product_path(product), class: "  inline-btn btn btn-success" %>
</div>
